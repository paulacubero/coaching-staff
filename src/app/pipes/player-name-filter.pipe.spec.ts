import { PlayerNameFilterPipe } from './player-name-filter.pipe';

describe('PlayerNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
